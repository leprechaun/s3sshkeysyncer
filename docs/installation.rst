============
Installation
============

At the command line::

    $ easy_install s3sshkeysyncer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv s3sshkeysyncer
    $ pip install s3sshkeysyncer
